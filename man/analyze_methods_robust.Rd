% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_methods.R
\name{analyze_methods_robust}
\alias{analyze_methods_robust}
\title{Run all five estimators (plus UNADJ) on one synthetic dataset}
\usage{
analyze_methods_robust(
  iteration_data,
  test_features = NULL,
  alpha = 0.05,
  debug = FALSE,
  n_cores = 1
)
}
\arguments{
\item{iteration_data}{Dataset list from \code{\link[=run_single_iteration]{run_single_iteration()}} (or
\code{\link[=generate_toy_data]{generate_toy_data()}}).}

\item{test_features}{Optional integer indices of outcome features to test.
Default \code{NULL} (all features).}

\item{alpha}{Significance threshold for the \code{significant} flag. Default 0.05.}

\item{debug}{If \code{TRUE}, message per-feature progress. Default \code{FALSE}.}

\item{n_cores}{Number of parallel workers. Default 1 (sequential).
Uses \code{parallel::mclapply} on Unix and a PSOCK cluster on Windows.}
}
\value{
Data frame: \code{feature}, \code{method}, \code{beta}, \code{se}, \code{pvalue}, \code{significant}.
}
\description{
Reference-named entry point: applies UNADJ, DIRECT, COCA, IV2SLS, and PGC
(matrix bridge) to each tested outcome feature and returns tidy per-feature
results with a significance flag.  Negative controls are summarised over
the full W panel for COCA, so restricting \code{test_features} does not change
its control summary.  The matrix-bridge PGC uses the full W panel
(or a validity-screened subset if provided).
}
\details{
A scalar-bridge variant (\code{\link[=fit_pgc_scalar]{fit_pgc_scalar()}}) is exported for standalone
use but is not included in the default pipeline.
}
