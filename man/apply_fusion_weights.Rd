% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/negative_controls.R
\name{apply_fusion_weights}
\alias{apply_fusion_weights}
\title{Apply FUSION/TWAS eQTL weights to a dosage matrix}
\usage{
apply_fusion_weights(
  genotypes,
  pos,
  weights_dir = NULL,
  weights = NULL,
  min_snps = 2,
  n_cores = 1
)
}
\arguments{
\item{genotypes}{Numeric dosage matrix, variants x samples, with
rownames matching the SNP IDs used in the weight files (usually
rsIDs).}

\item{pos}{A data.frame with (at least) columns \code{ID} (gene ID) and
\code{WGT} (weight-file name), as read from a FUSION \code{.pos} file; or a
path to such a file.}

\item{weights_dir}{Directory containing the \code{.wgt.RDat} files named
in \code{pos$WGT}. Ignored when \code{weights} is supplied.}

\item{weights}{Optional named list of pre-loaded weight objects,
each a list with elements \code{wgt.matrix}, \code{snps}, and
\code{cv.performance} (as stored in FUSION \code{.wgt.RDat} files). Names
must match \code{pos$ID}. Supplying this avoids repeated file I/O and
makes the function usable without the original files.}

\item{min_snps}{Integer: minimum number of matched SNPs required to
predict a gene. Default 2.}

\item{n_cores}{Integer: parallel workers via \code{\link[parallel:mclapply]{parallel::mclapply()}}.
Default 1.}
}
\value{
A list with:
\describe{
\item{predicted}{numeric matrix, genes x samples: predicted
expression.}
\item{info}{data.frame per predicted gene: \code{gene}, \code{n_snps},
\code{best_method}, \code{rsq} (cross-validation R2 of the selected
model).}
}
}
\description{
Predicts genetically regulated expression by applying published
FUSION per-gene weight files (\code{.wgt.RDat}) to a genotype dosage
matrix. For each gene, the model with the best cross-validation
performance (\code{cv.performance["rsq", ]}) is used. The resulting
predicted-expression matrix is germline-determined and — after PCA
via \code{\link[=build_w_pcs]{build_w_pcs()}} — serves as a negative-control panel that
captures confounding structure without direct exposure mediation.
}
\examples{
# Simulate two FUSION-style weight objects in memory
dos <- matrix(rbinom(10 * 50, 2, 0.3), nrow = 10,
              dimnames = list(paste0("rs", 1:10), paste0("S", 1:50)))
mk_wgt <- function(snps, w) {
  list(wgt.matrix = matrix(w, ncol = 1, dimnames = list(NULL, "enet")),
       snps = data.frame(V2 = snps),
       cv.performance = matrix(0.2, nrow = 1,
                               dimnames = list("rsq", "enet")))
}
wlist <- list(GeneA = mk_wgt(paste0("rs", 1:5), rep(0.1, 5)),
              GeneB = mk_wgt(paste0("rs", 6:10), rep(-0.2, 5)))
pos <- data.frame(ID = c("GeneA", "GeneB"), WGT = c("a.wgt.RDat", "b.wgt.RDat"))
out <- apply_fusion_weights(dos, pos = pos, weights = wlist)
dim(out$predicted)
}
