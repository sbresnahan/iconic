% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/negative_controls.R
\name{beta_to_m}
\alias{beta_to_m}
\title{Convert methylation beta values to M-values}
\usage{
beta_to_m(beta, clip = 1e-04, drop_nonfinite = TRUE)
}
\arguments{
\item{beta}{Numeric matrix of beta values (probes x samples), in
[0, 1].}

\item{clip}{Numeric: betas are clipped to [\code{clip}, \code{1 - clip}]
before the transform. Default 1e-4.}

\item{drop_nonfinite}{Logical: drop probes (rows) with any
missing/non-finite values after the transform. Default \code{TRUE}.}
}
\value{
A numeric matrix of M-values, probes x samples.
}
\description{
Applies the logit transform \eqn{\log_2(\beta / (1 - \beta))} after
clipping betas away from 0 and 1. M-values are approximately
homoscedastic and are the recommended scale for linear modeling and
PCA of methylation data.
}
\examples{
b <- matrix(runif(200, 0.01, 0.99), nrow = 20,
            dimnames = list(paste0("cg", 1:20), paste0("S", 1:10)))
m <- beta_to_m(b)
range(m)
}
