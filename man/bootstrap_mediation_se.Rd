% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mediation.R
\name{bootstrap_mediation_se}
\alias{bootstrap_mediation_se}
\title{Bootstrap SE for a mediation estimator}
\usage{
bootstrap_mediation_se(estimator_fn, n, n_boot = 500)
}
\arguments{
\item{estimator_fn}{A closure \verb{function(idx)} returning a fit list with
\code{NDE} and \code{NIE} (and optionally \code{NDE_se}, \code{NIE_se}).}

\item{n}{Sample size (length of the resampling index).}

\item{n_boot}{Number of bootstrap resamples. Default 500.}
}
\value{
A list with NDE_boot_se, NIE_boot_se, NDE_boot_dist, NIE_boot_dist.
}
\description{
Resamples the data \code{n_boot} times and returns the SD of the bootstrap
NDE/NIE distribution. This is an opt-in alternative to the delta-method
SE for users who want a non-parametric SE (
bootstrapped reasonably"). Slower than the delta method.
}
\details{
The estimator is supplied as a closure \code{estimator_fn(idx)} that captures
all model-specific data (outcome, exposure, mediator, instruments,
negative controls, covariates) and subsets each by \code{idx} internally.
This guarantees every resampled draw uses a synchronised bootstrap
sample across all variables — critical for the instrumented estimators
(IV2SLS, PGC, PGC2Gm) whose instruments and NC panels must be resampled
in lockstep with y, X, and M.
}
\keyword{internal}
