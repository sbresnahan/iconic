% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/instruments_mediator.R
\name{build_mediator_instruments}
\alias{build_mediator_instruments}
\title{Build mediator instruments via per-gene elastic net}
\usage{
build_mediator_instruments(
  expression,
  genotypes,
  gene_pos,
  snp_pos,
  covariates = NULL,
  cis_snps = NULL,
  alpha = 0.3,
  nfolds = 5,
  cis_dist = 1e+06,
  cv_r2_min = 0.005,
  cv_p_max = 0.1,
  n_cores = 1,
  seed = NULL,
  verbose = TRUE
)
}
\arguments{
\item{expression}{Numeric matrix, genes x samples (e.g.
inverse-normal-transformed TPM).}

\item{genotypes}{Numeric dosage matrix, variants x samples (0-2).}

\item{gene_pos}{data.frame with columns \code{gene}, \code{chr}, and \code{tss}
(or \code{start}).}

\item{snp_pos}{data.frame with columns \code{snp}, \code{chr}, \code{pos}.}

\item{covariates}{Optional data.frame or matrix (n rows) of
covariates to partial out (ancestry PCs, technical factors, ...).}

\item{cis_snps}{Optional named list: gene name -> character vector
of candidate SNP IDs (e.g. significant GTEx cis-eQTL pairs, as from
\code{\link[=call_cis_eqtls]{call_cis_eqtls()}} or an external resource). When supplied, the
elastic net for each gene sees only these SNPs (intersected with
the cis window).}

\item{alpha}{Numeric: elastic-net mixing (0 = ridge, 1 = lasso).
Default 0.3.}

\item{nfolds}{Integer: cross-validation folds. Default 5.}

\item{cis_dist}{Numeric: cis window half-width around the gene
start, in bp. Default 1e6.}

\item{cv_r2_min}{Numeric: minimum signed out-of-fold R2. Default
0.005.}

\item{cv_p_max}{Numeric: maximum one-sided out-of-fold correlation
p-value (nominal screen only; the load-bearing gates are
\code{cv_r2_min} and the non-degeneracy requirement). Default 0.10.}

\item{n_cores}{Integer: parallel workers via \code{\link[parallel:mclapply]{parallel::mclapply()}}.
Default 1.}

\item{seed}{Optional integer for reproducible CV fold assignments.}

\item{verbose}{Logical: print progress. Default \code{TRUE}.}
}
\value{
A list with:
\describe{
\item{Gm}{numeric matrix, genes x samples: in-sample predicted
expression for genes passing the quality filter. Pass to
\code{\link[=iconic_data]{iconic_data()}} as \code{Gm} (it is transposed internally).}
\item{qc}{data.frame of per-gene quality metrics: \code{gene},
\code{n_cis_snps}, \code{n_nonzero}, \code{cv_r2}, \code{cor_oof}, \code{cv_p},
\code{lambda_min}, \code{pass}.}
\item{weights}{named list (all trained genes) of data.frames with
\code{snp} and \code{weight} (non-zero coefficients at lambda.min).}
}
}
\description{
Trains one cross-validated elastic-net model per gene predicting
expression from cis SNPs, after residualizing both expression and
dosages on \code{covariates} (Frisch-Waugh-Lovell). Genes are kept when
the model is non-degenerate (at least one non-zero SNP weight), the
signed out-of-fold R2 exceeds \code{cv_r2_min}, and the one-sided
out-of-fold correlation p-value is below \code{cv_p_max}. The in-sample
predicted expression of passing genes is the composite mediator
instrument \code{Gm} consumed by \code{\link[=iconic_data]{iconic_data()}}.
}
\details{
Requires the glmnet package (listed under \code{Suggests}).
}
\examples{
if (requireNamespace("glmnet", quietly = TRUE)) {
  set.seed(42)
  n <- 150; p <- 80
  dos <- matrix(rbinom(p * n, 2, 0.3), nrow = p,
                dimnames = list(paste0("1:", 1:p, ":A:G"),
                                paste0("S", 1:n)))
  expr <- matrix(rnorm(4 * n), nrow = 4,
                 dimnames = list(paste0("Gene", 1:4), paste0("S", 1:n)))
  expr[1, ] <- expr[1, ] + 0.9 * scale(dos[5, ]) + 0.9 * scale(dos[15, ])
  gp <- data.frame(gene = paste0("Gene", 1:4), chr = "1",
                   tss = c(5, 25, 45, 65))
  sp <- data.frame(snp = rownames(dos), chr = "1", pos = 1:p)
  fit <- build_mediator_instruments(expr, dos, gp, sp, seed = 1)
  fit$qc
}
}
