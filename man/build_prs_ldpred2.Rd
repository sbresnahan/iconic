% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/instruments_exposure.R
\name{build_prs_ldpred2}
\alias{build_prs_ldpred2}
\title{Build an exposure polygenic score with LDpred2-auto}
\usage{
build_prs_ldpred2(
  sumstats,
  ld_ref,
  genotypes = NULL,
  hapmap3 = NULL,
  max_variants = NULL,
  n_chains = 30,
  p_range = c(1e-04, 0.2),
  burn_in = 500,
  num_iter = 500,
  shrink_corr = 0.95,
  allow_jump_sign = FALSE,
  use_mle = FALSE,
  chain_filter_quantile = 0.95,
  impute_mean = TRUE,
  ncores = 1,
  verbose = TRUE
)
}
\arguments{
\item{sumstats}{A data.frame of GWAS summary statistics, ideally
pre-processed by \code{\link[=qc_gwas_sumstats]{qc_gwas_sumstats()}}. Must contain (after
standardization) \code{chr}, \code{pos}, \code{a0}, \code{a1}, \code{beta}, \code{beta_se},
\code{n_eff}; \code{freq} is used for the SD-ratio QC when present.}

\item{ld_ref}{Either a path to a bigsnpr \code{.rds} backing file (from
\code{\link[bigsnpr:snp_readBed]{bigsnpr::snp_readBed()}}) or an attached \code{bigSNP} object. Should
match the ancestry of the GWAS.}

\item{genotypes}{Optional: path to a bigsnpr \code{.rds} or an attached
\code{bigSNP} object for the target cohort to be scored. When \code{NULL}
(default), only the LDpred2 effect sizes are returned.}

\item{hapmap3}{Optional character vector of variant IDs (rsIDs or
\code{chr:pos:a0:a1} keys) to restrict the analysis to (e.g. HapMap3).
Restriction improves chain stability; \code{NULL} (default) uses all
matched variants.}

\item{max_variants}{Optional integer: if the matched variant count
exceeds this, systematically thin to approximately \code{max_variants}
variants (evenly spaced along the genome) before computing LD.
Default \code{NULL} (no thinning). Use e.g. \code{250000} to bound memory.}

\item{n_chains}{Integer: number of LDpred2-auto chains. Default 30.}

\item{p_range}{Numeric length-2: range of the initial per-chain
proportion-of-causal-variants grid, log-spaced. Default
\code{c(1e-4, 0.2)}.}

\item{burn_in}{Integer: burn-in iterations per chain. Default 500.}

\item{num_iter}{Integer: sampling iterations per chain (after
burn-in). Default 500.}

\item{shrink_corr}{Numeric: LD shrinkage parameter. Default 0.95;
reduce to ~0.4 when the SD-ratio QC flags >10\% of variants.}

\item{allow_jump_sign}{Logical: allow sign jumps across chains.
Default \code{FALSE} (recommended for auto mode).}

\item{use_mle}{Logical: use MLE for the hyperparameter updates.
Default \code{FALSE} (appropriate for moderate GWAS sample sizes).}

\item{chain_filter_quantile}{Numeric: chains are kept when the range
of their posterior \code{corr_est} exceeds
\code{chain_filter_quantile}-quantile of all chain ranges times 0.95.
Default 0.95.}

\item{impute_mean}{Logical: mean-impute missing target genotypes
before scoring. Default \code{TRUE}.}

\item{ncores}{Integer: threads for bigsnpr. Default 1.}

\item{verbose}{Logical: print progress. Default \code{TRUE}.}
}
\value{
A list with:
\describe{
\item{beta}{data.frame of LDpred2 effect sizes (\code{chr}, \code{pos},
\code{a0}, \code{a1}, \code{beta}) in LD-reference orientation.}
\item{score}{numeric vector of per-sample polygenic scores for
\code{genotypes} (scaled to unit variance), or \code{NULL} when \code{genotypes}
was not supplied.}
\item{h2}{LD-score-regression SNP heritability estimate used to
initialize the chains.}
\item{chains_kept}{integer indices of chains passing the
convergence filter.}
\item{qc}{list with variant counts at each stage.}
}
}
\description{
Runs the LDpred2-auto Bayesian shrinkage pipeline (Privé et al.) via
the bigsnpr package: match summary statistics to an LD reference,
estimate SNP heritability by LD score regression, run a grid of
auto chains, filter chains by convergence, and average the
per-chain effects. Optionally scores a target genotype set.
}
\details{
This function requires the bigsnpr package (listed under \code{Suggests})
and a PLINK/bed-format LD reference panel converted with
\code{\link[bigsnpr:snp_readBed]{bigsnpr::snp_readBed()}}.
}
\examples{
if (requireNamespace("bigsnpr", quietly = TRUE)) {
  # Tiny fake genotype panel standing in for an LD reference
  # (snp_fake genotypes are all-missing mocks; fill with random dosages)
  fake <- bigsnpr::snp_fake(100, 500)
  fake$genotypes[] <- rbinom(100 * 500, 2, 0.3)
  fake$map$chromosome <- 1L
  fake$map$physical.pos <- sort(sample(1:1e6, 500))
  ss <- data.frame(
    chr = fake$map$chromosome, pos = fake$map$physical.pos,
    a0 = fake$map$allele2, a1 = fake$map$allele1,
    beta = rnorm(500, 0, 0.05), beta_se = 0.02, n_eff = 20000
  )
  prs <- build_prs_ldpred2(ss, ld_ref = fake, n_chains = 3,
                           burn_in = 50, num_iter = 50, verbose = FALSE)
  str(prs$beta)
}
}
