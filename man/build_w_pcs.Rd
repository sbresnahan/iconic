% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/negative_controls.R
\name{build_w_pcs}
\alias{build_w_pcs}
\title{Build a negative-control panel from principal components}
\usage{
build_w_pcs(x, n_pcs = 20, scale_features = FALSE, prefix = "PC")
}
\arguments{
\item{x}{Numeric matrix, features x samples (e.g. residualized
M-values or predicted expression).}

\item{n_pcs}{Integer: number of PCs to return. Default 20.}

\item{scale_features}{Logical: scale features to unit variance
before PCA. Default \code{FALSE} (appropriate for M-values); use \code{TRUE}
for predicted-expression panels where feature scales differ.}

\item{prefix}{Character: row-name prefix for the PCs. Default
\code{"PC"}.}
}
\value{
A list with:
\describe{
\item{W}{numeric matrix, PCs x samples, suitable as the \code{W}
argument of \code{\link[=iconic_data]{iconic_data()}}.}
\item{variance_explained}{numeric vector: percent of total
variance explained by each returned PC.}
}
}
\description{
Computes the top \code{n_pcs} principal components of a features x
samples matrix (typically residualized with
\code{\link[=residualize_matrix]{residualize_matrix()}}) and returns the sample scores as a W panel
(PCs x samples) in the orientation expected by \code{\link[=iconic_data]{iconic_data()}}.
Uses irlba for fast truncated PCA when available, falling back to
\code{\link[stats:prcomp]{stats::prcomp()}}.
}
\examples{
x <- matrix(rnorm(500 * 60), nrow = 500,
            dimnames = list(paste0("f", 1:500), paste0("S", 1:60)))
w <- build_w_pcs(x, n_pcs = 5)
dim(w$W)
w$variance_explained
}
