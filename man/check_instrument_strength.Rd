% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/instruments_exposure.R
\name{check_instrument_strength}
\alias{check_instrument_strength}
\title{Check first-stage instrument strength (partial F)}
\usage{
check_instrument_strength(G, X, covariates = NULL, min_f = 10)
}
\arguments{
\item{G}{Numeric vector (length n): the exposure instrument
(e.g. a polygenic score from \code{\link[=build_prs_ldpred2]{build_prs_ldpred2()}} or
\code{\link[=score_pgs_panel]{score_pgs_panel()}}).}

\item{X}{Numeric vector (length n): the exposure.}

\item{covariates}{Optional data.frame or matrix (n rows) of
covariates to partial out (e.g. ancestry PCs).}

\item{min_f}{Numeric: weak-instrument threshold on the partial F.
Default 10 (Staiger-Stock rule of thumb).}
}
\value{
A list with \code{F} (partial F statistic), \code{df1}, \code{df2},
\code{pvalue}, \code{partial_r2}, \code{n}, and \code{weak} (\code{TRUE} when F < \code{min_f}).
}
\description{
Regresses the exposure \code{X} on the instrument \code{G} (plus optional
covariates) and reports the partial F statistic and partial R2 for
the instrument — the weak-instrument diagnostic. The partial F
(not the overall model F) is the relevant quantity when covariates
such as ancestry PCs explain much of the exposure.
}
\examples{
n <- 300
G <- rnorm(n)
X <- 0.3 * G + rnorm(n)
pcs <- matrix(rnorm(n * 3), n, 3, dimnames = list(NULL, paste0("PC", 1:3)))
check_instrument_strength(G, X, covariates = pcs)
}
