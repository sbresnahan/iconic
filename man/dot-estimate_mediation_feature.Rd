% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mediation.R
\name{.estimate_mediation_feature}
\alias{.estimate_mediation_feature}
\title{Estimate mediation effects for a single feature with specified methods (internal)}
\usage{
.estimate_mediation_feature(
  Z,
  y,
  M_vec,
  g = NULL,
  gm = NULL,
  w = NULL,
  W_mat = NULL,
  W1_mat = NULL,
  W2_mat = NULL,
  W_avg = NULL,
  covars = NULL,
  methods = NULL,
  feature_idx = 1L,
  min_f = 10,
  se_method = c("delta", "bootstrap", "composite"),
  n_boot = 500
)
}
\arguments{
\item{Z}{Numeric exposure vector (length n).}

\item{y}{Numeric outcome vector (length n).}

\item{M_vec}{Numeric mediator vector (length n).}

\item{g}{Numeric instrument vector (length n), or NULL.}

\item{gm}{Numeric mediator instrument vector (length n), or NULL.}

\item{w}{Numeric NC vector (length n) or matrix (n x q), or NULL
(for DIRECT, IV2SLS, IV2SLS2 — full panel as covariates).}

\item{W_mat}{Numeric NC matrix (n x q), or NULL (for PGC matrix bridge).}

\item{W1_mat}{Numeric path-specific NC matrix (n x q) for Z->M, or NULL.}

\item{W2_mat}{Numeric path-specific NC matrix (n x q) for M->Y, or NULL.}

\item{W_avg}{Numeric vector (length n): row means of NC panel for COCA.
If NULL but W_mat present, computed inline.}

\item{covars}{Optional data frame of covariates (n rows).}

\item{methods}{Character vector of methods to run. Default: all applicable.}

\item{feature_idx}{Integer or character: feature identifier. Default 1L.}

\item{se_method}{"delta" (default), "bootstrap", or "composite" (v0.9.3).
When "bootstrap", NDE_se/NIE_se are replaced by the SD of \code{n_boot}
nonparametric bootstrap resamples (JYH #867). Point estimates (NDE/NIE)
and p-values are unchanged; only the SE column is swapped.
When "composite", NIE_p is replaced by the Huang (2019) JT-comp
composite null p-value (post-processed across features by the driver).}

\item{n_boot}{Number of bootstrap resamples when \code{se_method="bootstrap"}.}
}
\value{
Data frame: \code{feature}, \code{method}, \code{NDE}, \code{NDE_se}, \code{NDE_p},
\code{NIE}, \code{NIE_se}, \code{NIE_p}.  Returns NULL if < 20 complete cases.
When \code{se_method="composite"}, also includes \code{alpha_M}, \code{alpha_se},
\code{beta_M}, \code{beta_M_se} for downstream composite p-value computation.
}
\description{
Format-agnostic per-feature mediation estimation: takes explicit vectors
and covariates (no dependency on the \code{dat} list format).  Called by both
the simulation driver (.analyze_mediation_feature) and the real-data
driver (iconic_estimate).
}
\keyword{internal}
