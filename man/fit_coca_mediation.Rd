% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mediation.R
\name{fit_coca_mediation}
\alias{fit_coca_mediation}
\title{COCA mediation estimator: negative-control calibration of both stages}
\usage{
fit_coca_mediation(y, Z, M, w, covars = NULL, ratio_cap = 10)
}
\arguments{
\item{y}{Numeric primary outcome vector (length n).}

\item{Z}{Numeric exposure vector (length n).}

\item{M}{Numeric mediator vector (length n).}

\item{w}{Numeric negative-control outcome vector (length n).
Recommended: pass \code{rowMeans(W_matrix)} for stability.}

\item{covars}{Optional data frame of additional covariates (n rows).}

\item{ratio_cap}{Maximum absolute value of any ratio estimate before
flagging as unstable. Default 10.}
}
\value{
Named list: \code{NDE}, \code{NDE_se}, \code{NDE_p},
\code{NIE}, \code{NIE_se}, \code{NIE_p}.
}
\description{
Uses the negative-control outcome W to calibrate both the mediator
and outcome regressions, extending \code{\link{fit_coca}} to the
mediation setting.
}
\details{
Stage 1: \code{W ~ M + Z} -> calibrated alpha_M = -beta_Z / beta_M.
Stage 2: \code{W ~ Y + Z + M} -> calibrated NDE = -beta_Z / beta_Y,
calibrated beta_M = -beta_M / beta_Y.
NIE = alpha_M * beta_M (both calibrated).
}
\examples{
\dontrun{
set.seed(1)
dat <- iconic:::generate_toy_data(n = 200, mo_confounding = 0.8, seed = 1)
fit_coca_mediation(dat$Y[, 1], dat$Z, dat$M, rowMeans(dat$W))
}
}
