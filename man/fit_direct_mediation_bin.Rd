% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bin_mediation.R
\name{fit_direct_mediation_bin}
\alias{fit_direct_mediation_bin}
\title{DIRECT binary mediation estimator: logistic / LPM with G and W covariates}
\usage{
fit_direct_mediation_bin(
  y,
  X,
  M,
  g,
  w,
  covars = NULL,
  effect_scale = c("logor", "riskdiff")
)
}
\arguments{
\item{y}{Numeric 0/1 outcome vector (length n).}

\item{X}{Numeric exposure vector (length n).}

\item{M}{Numeric mediator vector (length n).}

\item{g}{Numeric instrument vector (length n).}

\item{w}{Numeric NC vector (length n) or matrix (n x q).}

\item{covars}{Optional data frame of covariates (n rows).}

\item{effect_scale}{Character: \code{"logor"} or \code{"riskdiff"}.}
}
\value{
Named list (same fields as \code{\link{fit_unadj_mediation_bin}}).
}
\description{
Adjusts for the instrument G and negative-control W in both the
mediator (OLS) and outcome (logistic / LPM) stages. Naive adjustment.
}
\examples{
set.seed(1)
dat <- generate_toy_data(n = 200, outcome_type = "binary",
mo_confounding = 0.8, seed = 1)
fit_direct_mediation_bin(dat$y_bin, dat$X, dat$M,
dat$G[, 1], dat$W[, 1])
}
