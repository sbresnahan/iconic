% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/surv_mediation.R
\name{fit_direct_mediation_surv}
\alias{fit_direct_mediation_surv}
\title{DIRECT survival mediation estimator: Cox / RMST with G and W covariates}
\usage{
fit_direct_mediation_surv(
  time,
  event,
  X,
  M,
  g,
  w,
  covars = NULL,
  effect_scale = c("loghr", "rmst"),
  tau = NULL
)
}
\arguments{
\item{time}{Numeric follow-up time vector (length n).}

\item{event}{Numeric 0/1 event indicator (length n).}

\item{X}{Numeric exposure vector (length n).}

\item{M}{Numeric mediator vector (length n).}

\item{g}{Numeric instrument vector (length n).}

\item{w}{Numeric NC vector (length n) or matrix (n x q).}

\item{covars}{Optional data frame of covariates (n rows).}

\item{effect_scale}{Character: \code{"loghr"} or \code{"rmst"}.}

\item{tau}{RMST horizon (rmst only).}
}
\value{
Named list (same fields as \code{\link{fit_unadj_mediation_surv}}).
}
\description{
Adjusts for the instrument G and negative-control W in both the
mediator (OLS) and outcome (Cox / RMST) stages. Naive adjustment.
}
\examples{
\donttest{
set.seed(1)
dat <- iconic:::generate_toy_data(n = 200, outcome_type = "survival",
mo_confounding = 0.8, seed = 1)
fit_direct_mediation_surv(dat$surv_time, dat$surv_event, dat$X, dat$M,
dat$G[, 1], dat$W[, 1])
}
}
