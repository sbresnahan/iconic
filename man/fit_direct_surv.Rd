% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/surv_estimators.R
\name{fit_direct_surv}
\alias{fit_direct_surv}
\title{DIRECT survival estimator: Cox / RMST with instrument and NC covariates}
\usage{
fit_direct_surv(
  time,
  event,
  Z,
  g,
  w,
  covars = NULL,
  effect_scale = c("loghr", "rmst"),
  tau = NULL
)
}
\arguments{
\item{time}{Numeric follow-up time vector (length n).}

\item{event}{Numeric 0/1 event indicator (length n).}

\item{Z}{Numeric exposure vector (length n).}

\item{g}{Numeric instrument vector (length n).}

\item{w}{Numeric NC vector (length n) or matrix (n x q).}

\item{covars}{Optional data frame of covariates (n rows).}

\item{effect_scale}{Character: \code{"loghr"} or \code{"rmst"}.}

\item{tau}{RMST horizon (rmst only).}
}
\value{
Named list: \code{beta}, \code{se}, \code{pvalue}.
}
\description{
Regresses the survival outcome on Z plus the genetic instrument G, the
negative-control panel W, and covariates.  Naive adjustment; does not
correct for unmeasured confounding via a ratio or IV approach.
}
\examples{
\donttest{
set.seed(1)
dat <- iconic:::generate_toy_data(n = 200, outcome_type = "survival", seed = 1)
fit_direct_surv(dat$surv_time, dat$surv_event, dat$Z, dat$G[, 1], dat$W[, 1])
}
}
