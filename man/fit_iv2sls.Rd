% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/estimators.R
\name{fit_iv2sls}
\alias{fit_iv2sls}
\title{IV2SLS estimator: Two-Stage Least Squares with genetic instrument}
\usage{
fit_iv2sls(y, Z, g, w, covars = NULL, min_f = 10)
}
\arguments{
\item{y}{Numeric outcome vector (length n).}

\item{Z}{Numeric exposure vector (length n).}

\item{g}{Numeric instrument vector (length n).}

\item{w}{Numeric negative-control vector (length n).}

\item{covars}{Optional data frame of additional covariates (n rows).}

\item{min_f}{Minimum acceptable partial F-statistic for the excluded
instrument. Default 10.}
}
\value{
Named list: \code{beta}, \code{se}, \code{pvalue}.
}
\description{
Uses the genetic instrument G to instrument for the exposure Z,
controlling for the negative-control W and any additional covariates.
Requires \pkg{AER}.
}
\details{
A weak-instrument check is applied using the partial F-statistic for the
excluded instrument G (testing G conditional on W and covariates in the
first stage), following Stock & Yogo (2005).  If the partial F is below
\code{min_f}, the function returns \code{NA}.
}
\examples{
\donttest{
set.seed(1)
dat <- iconic:::generate_toy_data(n = 200, seed = 1)
fit_iv2sls(dat$Y[, 1], dat$Z, dat$G[, 1], dat$W[, 1])
}
}
