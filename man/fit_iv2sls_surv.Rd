% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/surv_estimators.R
\name{fit_iv2sls_surv}
\alias{fit_iv2sls_surv}
\title{IV2SLS survival estimator: two-stage predictor substitution with Cox / RMST}
\usage{
fit_iv2sls_surv(
  time,
  event,
  X,
  g,
  w,
  covars = NULL,
  min_f = 10,
  effect_scale = c("loghr", "rmst"),
  tau = NULL
)
}
\arguments{
\item{time}{Numeric follow-up time vector (length n).}

\item{event}{Numeric 0/1 event indicator (length n).}

\item{X}{Numeric exposure vector (length n).}

\item{g}{Numeric instrument vector (length n).}

\item{w}{Numeric NC vector (length n) or matrix (n x q).}

\item{covars}{Optional data frame of covariates (n rows).}

\item{min_f}{Minimum partial F for the excluded instrument. Default 10.}

\item{effect_scale}{Character: \code{"loghr"} or \code{"rmst"}.}

\item{tau}{RMST horizon (rmst only).}
}
\value{
Named list: \code{beta}, \code{se}, \code{pvalue}.
}
\description{
Two-stage predictor substitution (2SPS): the first stage regresses X on
the instrument G (plus W and covariates) via OLS, producing fitted
\eqn{\hat X}; the second stage regresses the survival outcome on
\eqn{\hat X} (plus W and covariates) via Cox (log-HR) or RMST
pseudo-observation OLS.
}
\details{
A weak-instrument check (partial F for the excluded instrument G,
Stock & Yogo 2005) is applied to the OLS first stage. If the partial
F is below \code{min_f}, the function returns NA.
}
\examples{
set.seed(1)
dat <- generate_toy_data(n = 200, outcome_type = "survival", seed = 1)
fit_iv2sls_surv(dat$surv_time, dat$surv_event, dat$X, dat$G[, 1], dat$W[, 1])
}
