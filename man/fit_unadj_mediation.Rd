% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mediation.R
\name{fit_unadj_mediation}
\alias{fit_unadj_mediation}
\title{UNADJ mediation estimator: naive Baron-Kenny style}
\usage{
fit_unadj_mediation(y, X, M, covars = NULL)
}
\arguments{
\item{y}{Numeric outcome vector (length n).}

\item{X}{Numeric exposure vector (length n).}

\item{M}{Numeric mediator vector (length n).}

\item{covars}{Optional data frame of additional covariates (n rows).}
}
\value{
Named list: \code{NDE}, \code{NDE_se}, \code{NDE_p},
\code{NIE}, \code{NIE_se}, \code{NIE_p}.
}
\description{
Stage 1: \code{M ~ X} (estimate alpha_M).
Stage 2: \code{Y ~ X + M} (estimate NDE = beta_X, beta_M).
NIE = alpha_M * beta_M.
}
\details{
Does not adjust for unmeasured confounding. Provided as a bias
reference floor, analogous to UNADJ in the total-effect setting.
}
\examples{
set.seed(1)
dat <- generate_toy_data(n = 200, mo_confounding = 0.8, seed = 1)
fit_unadj_mediation(dat$Y[, 1], dat$X, dat$M)
}
