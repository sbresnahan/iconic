% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bin_mediation.R
\name{fit_unadj_mediation_bin}
\alias{fit_unadj_mediation_bin}
\title{UNADJ binary mediation estimator: naive Baron-Kenny with logistic / LPM}
\usage{
fit_unadj_mediation_bin(
  y,
  X,
  M,
  covars = NULL,
  effect_scale = c("logor", "riskdiff")
)
}
\arguments{
\item{y}{Numeric 0/1 outcome vector (length n).}

\item{X}{Numeric exposure vector (length n).}

\item{M}{Numeric mediator vector (length n).}

\item{covars}{Optional data frame of covariates (n rows).}

\item{effect_scale}{Character: \code{"logor"} or \code{"riskdiff"}.}
}
\value{
Named list: \code{NDE}, \code{NDE_se}, \code{NDE_p},
\code{NIE}, \code{NIE_se}, \code{NIE_p}, \code{alpha_M},
\code{alpha_se}, \code{beta_M}, \code{beta_M_se}.
}
\description{
Stage 1 (OLS): \code{M ~ X} -> alpha_M. Stage 2 (logistic / LPM):
binary outcome ~ X + M -> NDE (coef on X), beta_M (coef on M).
\code{NIE = alpha_M * beta_M}. No confounding adjustment; bias
reference.
}
\examples{
set.seed(1)
dat <- generate_toy_data(n = 200, outcome_type = "binary",
mo_confounding = 0.8, seed = 1)
fit_unadj_mediation_bin(dat$y_bin, dat$X, dat$M)
}
