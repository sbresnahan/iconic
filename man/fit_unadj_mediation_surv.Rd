% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/surv_mediation.R
\name{fit_unadj_mediation_surv}
\alias{fit_unadj_mediation_surv}
\title{UNADJ survival mediation estimator: naive Baron-Kenny with Cox / RMST}
\usage{
fit_unadj_mediation_surv(
  time,
  event,
  Z,
  M,
  covars = NULL,
  effect_scale = c("loghr", "rmst"),
  tau = NULL
)
}
\arguments{
\item{time}{Numeric follow-up time vector (length n).}

\item{event}{Numeric 0/1 event indicator (length n).}

\item{Z}{Numeric exposure vector (length n).}

\item{M}{Numeric mediator vector (length n).}

\item{covars}{Optional data frame of covariates (n rows).}

\item{effect_scale}{Character: \code{"loghr"} or \code{"rmst"}.}

\item{tau}{RMST horizon (rmst only).}
}
\value{
Named list: \code{NDE}, \code{NDE_se}, \code{NDE_p},
\code{NIE}, \code{NIE_se}, \code{NIE_p}, \code{alpha_M},
\code{alpha_se}, \code{beta_M}, \code{beta_M_se}.
}
\description{
Stage 1 (OLS): \code{M ~ Z} -> alpha_M.  Stage 2 (Cox / RMST):
survival outcome ~ Z + M -> NDE (coef on Z), beta_M (coef on M).
\code{NIE = alpha_M * beta_M}.  No confounding adjustment; bias
reference.
}
\examples{
\dontrun{
set.seed(1)
dat <- iconic:::generate_toy_data(n = 200, outcome_type = "survival",
                                  mo_confounding = 0.8, seed = 1)
fit_unadj_mediation_surv(dat$surv_time, dat$surv_event, dat$Z, dat$M)
}
}
