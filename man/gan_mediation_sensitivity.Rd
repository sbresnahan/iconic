% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sensitivity.R
\name{gan_mediation_sensitivity}
\alias{gan_mediation_sensitivity}
\title{Benchmark mediation estimators across confounding scenarios}
\usage{
gan_mediation_sensitivity(
  trained_gan = NULL,
  conf_grid = c(0.2, 0.5, 0.8),
  coverage_grid = c(0.3, 0.7, 1),
  k_grid = 1,
  mo_confounding = 0.8,
  phi = 0,
  rho_G1 = 0,
  rho_G2 = 0,
  rho_pop = 0,
  lambda_XM = NULL,
  lambda_MY = NULL,
  omega_1 = NULL,
  omega_2 = NULL,
  nc_model = "proxy",
  n_iter = 50,
  n_samples = 500,
  n_features = 20,
  beta_X = 0.1,
  alpha_M = 0.5,
  beta_M = 0.3,
  base_seed = 750,
  n_cores = 1,
  outcome_type = c("continuous", "survival"),
  effect_scale = c("loghr", "rmst"),
  surv_h0 = 0.1,
  surv_event_frac = 0.6,
  surv_censor_rate = NULL
)
}
\arguments{
\item{trained_gan}{An \code{iconic_gan} (or \code{NULL} to use default texture).}

\item{conf_grid}{Confounding-strength values to sweep. Default \code{c(0.2, 0.5, 0.8)}.}

\item{coverage_grid}{Negative-control coverage values in \verb{[0,1]}. Default \code{c(0.3, 0.7, 1)}.}

\item{k_grid}{Numbers of latent confounders to sweep. Default \code{1}.}

\item{mo_confounding}{Strength of U1 -> M (mediator-outcome confounding). Default 0.80.}

\item{phi}{Strength of the mediator instrument Gm -> M.
0 = no mediator instrument (five estimators). > 0 =
generates Gm and includes the 2-stage MR estimator
(IV2SLS2). Default 0.}

\item{rho_G1}{Correlation of G1 with conf_XM. Default 0.}

\item{rho_G2}{Correlation of G2 with conf_MY. Default 0.}

\item{rho_pop}{Shared population structure. Default 0.}

\item{lambda_XM}{Optional per-path confounder loading vector (X->M path).}

\item{lambda_MY}{Optional per-path confounder loading vector (M->Y path).}

\item{omega_1}{Coverage of conf_XM by W1. NULL = use \code{coverage}.}

\item{omega_2}{Coverage of conf_MY by W2. NULL = use \code{coverage}.}

\item{nc_model}{Negative-control model (function or name). Default \code{"proxy"}.}

\item{n_iter}{Replicates per scenario. Default 50.}

\item{n_samples}{Samples per replicate. Default 500.}

\item{n_features}{Features per replicate. Default 20.}

\item{beta_X, alpha_M, beta_M}{Causal paths (ground truth). Defaults 0.10 / 0.50 / 0.30.}

\item{base_seed}{Base RNG seed. Default 750.}

\item{n_cores}{Parallel workers across replicates. Default 1.}

\item{outcome_type}{\code{"continuous"} (default) or \code{"survival"}
When survival, the DGP generates time-to-event outcomes and
estimation uses the Cox / RMST survival mediation drivers via
\code{\link[=iconic_estimate]{iconic_estimate()}}.}

\item{effect_scale}{\code{"loghr"} (default) or \code{"rmst"}. Only
used when \code{outcome_type = "survival"}.}

\item{surv_h0}{Baseline hazard for survival DGP. See
\code{\link[=run_single_iteration]{run_single_iteration()}}.}

\item{surv_event_frac}{Target event fraction for survival DGP.}

\item{surv_censor_rate}{Censoring rate for survival DGP.}
}
\value{
A list with \code{summary} (one row per scenario x method, with
\code{conf_strength}, \code{coverage}, \code{k}, \code{mo_confounding}, \code{phi}, \code{true_NDE},
\code{true_NIE} and NDE/NIE bias/RMSE/Type I columns) and \code{grid}.
}
\description{
For each scenario in the grid, generates \code{n_iter} synthetic datasets
with mediator-outcome confounding (via \code{mo_confounding}), runs every
mediation estimator, and summarises NDE/NIE bias / RMSE / Type I error.
This is the mediation analogue of \code{\link[=gan_sensitivity]{gan_sensitivity()}}.
}
\details{
When \code{phi > 0}, a mediator-specific genetic instrument (Gm) is
generated and the 2-stage MR estimator (IV2SLS2) is included in the
results, enabling point identification of NDE/NIE under M-O confounding.
}
\examples{
\dontrun{
sens <- gan_mediation_sensitivity(NULL, conf_grid = c(0.3, 0.8),
mo_confounding = 0.8, n_iter = 20)
head(sens$summary)
}
}
