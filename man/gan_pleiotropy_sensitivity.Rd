% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pleiotropy_sensitivity.R
\name{gan_pleiotropy_sensitivity}
\alias{gan_pleiotropy_sensitivity}
\title{Benchmark estimators across pleiotropy and confounding scenarios}
\usage{
gan_pleiotropy_sensitivity(
  trained_gan = NULL,
  pleio_grid = c(0, 0.05, 0.1),
  conf_grid = c(0.2, 0.5, 0.8),
  tau = 0.25,
  nc_model = "proxy",
  n_iter = 50,
  n_samples = 500,
  n_features = 10,
  coverage = 0.7,
  k = 1,
  base_seed = 900,
  n_cores = 1
)
}
\arguments{
\item{trained_gan}{An \code{iconic_gan} (or \code{NULL} to use default texture).}

\item{pleio_grid}{Horizontal-pleiotropy strengths (direct G -> Y
coefficients) to sweep. Default \code{c(0, 0.05, 0.10)}.}

\item{conf_grid}{Confounding-strength values. Default \code{c(0.2, 0.5, 0.8)}.}

\item{tau}{True total effect for the alternative arm. Default 0.25.}

\item{nc_model}{Negative-control model (function or name). Default \code{"proxy"}.}

\item{n_iter}{Replicates per cell per arm. Default 50.}

\item{n_samples}{Samples per replicate. Default 500.}

\item{n_features}{Features per replicate. Default 10.}

\item{coverage}{Negative-control coverage. Default 0.7.}

\item{k}{Number of latent confounders. Default 1.}

\item{base_seed}{Base RNG seed. Default 900.}

\item{n_cores}{Parallel workers across replicates. Default 1.}
}
\value{
A list with \code{summary} (one row per cell x arm x method, with
\code{pleio}, \code{conf_strength}, \code{arm}, \code{true_total}, and the columns from
\code{summarise_results()}) and \code{grid}.
}
\description{
For each cell in the grid (pleiotropy strength x confounding strength),
generates \code{n_iter} synthetic datasets with \code{\link[=run_single_iteration]{run_single_iteration()}},
runs every estimator, and summarises bias / RMSE / power. Two arms are
run per cell:
}
\details{
\itemize{
\item \strong{alternative} (\code{effect_size = tau}): the true total effect is \code{tau},
so \code{power} is the empirical power to detect it.
\item \strong{null} (\code{effect_size = 0}): the true total effect is 0, so \code{power}
is the empirical Type I error rate.
}

The \code{pleio} parameter adds a direct \code{G -> Y} path of the requested
strength, violating the exclusion restriction. IV/2SLS is consistent
only when \code{pleio = 0}; any \code{pleio > 0} introduces bias that does not
shrink with sample size.
}
\examples{
\dontrun{
sens <- gan_pleiotropy_sensitivity(NULL,
pleio_grid = c(0, 0.05, 0.10), conf_grid = c(0.5, 0.8),
n_iter = 20, n_features = 10)
head(sens$summary)
}
}
