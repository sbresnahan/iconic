% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sensitivity.R
\name{gan_sensitivity}
\alias{gan_sensitivity}
\title{Benchmark estimators across confounding scenarios on synthetic data}
\usage{
gan_sensitivity(
  trained_gan = NULL,
  conf_grid = c(0.2, 0.5, 0.8),
  coverage_grid = c(0.3, 0.7, 1),
  k_grid = 1,
  nc_model = "proxy",
  n_iter = 50,
  n_samples = 500,
  n_features = 20,
  beta_Z = 0.1,
  alpha_M = 0.5,
  beta_M = 0.3,
  effect_size = NULL,
  base_seed = 700,
  n_cores = 1
)
}
\arguments{
\item{trained_gan}{An \code{iconic_gan} (or \code{NULL} to use default texture).}

\item{conf_grid}{Confounding-strength values to sweep. Default \code{c(0.2, 0.5, 0.8)}.}

\item{coverage_grid}{Negative-control coverage values in \verb{[0,1]}. Default \code{c(0.3, 0.7, 1)}.}

\item{k_grid}{Numbers of latent confounders to sweep. Default \code{1}.}

\item{nc_model}{Negative-control model (function or name). Default \code{"proxy"}.}

\item{n_iter}{Replicates per scenario. Default 50.}

\item{n_samples}{Samples per replicate. Default 500.}

\item{n_features}{Features per replicate. Default 20.}

\item{beta_Z, alpha_M, beta_M}{Causal paths (ground truth). Defaults 0.10 / 0.50 / 0.30.}

\item{effect_size}{Optional pure-direct total effect override (see
\code{\link[=run_single_iteration]{run_single_iteration()}}).}

\item{base_seed}{Base RNG seed. Default 700.}

\item{n_cores}{Parallel workers across replicates. Default 1.}
}
\value{
A list with \code{summary} (one row per scenario x method, with
\code{conf_strength}, \code{coverage}, \code{k}, \code{true_total} and the columns from
\code{summarise_results}) and \code{grid} (the scenario grid).
}
\description{
For each scenario in the grid, generates \code{n_iter} synthetic datasets with
\code{\link[=run_single_iteration]{run_single_iteration()}} (using the trained generator for texture), runs every
estimator, and summarises bias / RMSE / power. This is the multi-confounder,
negative-control-aware generalisation of the package's parameter sweeps.
}
