% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/load_data.R
\name{load_real_input_data}
\alias{load_real_input_data}
\title{Load and standardise a real multi-omic dataset for GAN training}
\usage{
load_real_input_data(
  X_matrix = NULL,
  Y_matrix = NULL,
  M_matrix = NULL,
  W_matrix = NULL,
  covariates_df = NULL,
  example = FALSE,
  marginal_method = "auto",
  residualize_on = c("XC", "XCW"),
  Z_matrix = NULL
)
}
\arguments{
\item{X_matrix}{Exposure panel, \verb{features x samples} (e.g. an exposure
or molecular score across probes). Column means become the per-sample exposure.}

\item{Y_matrix}{Outcome, either a scalar vector of length \code{n_samples}
(e.g. a clinical or molecular trait) or a \verb{features x samples} matrix.
When a vector, it defines \code{n_samples}; when a matrix, \code{n_samples} and
\code{n_features}.}

\item{M_matrix}{Mediator panel, \verb{features x samples} (e.g. gene or
transcript expression). When supplied, its per-sample summary is added to
the GAN training frame (so the generator learns the mediator's marginal
distribution) and its residual correlation matrix is computed and stored.}

\item{W_matrix}{Optional negative-control panel, \verb{features x samples}.
When supplied the controls are treated as user-provided real structure;
when \code{NULL} they are generated by a negative-control model at simulation time.}

\item{covariates_df}{Optional data frame of sample-level covariates. A
\code{sample_id} column is used to align samples if present; recognised columns
\code{sex}, \code{GA}, \code{mother_ethnicity} are encoded, and any other numeric columns
are z-scored and kept. Names colliding with estimator-reserved tokens are
renamed with a \code{cov_} prefix.}

\item{example}{Force return of the built-in example dataset. Default
\code{FALSE} (auto-enabled when no matrices are given).}

\item{marginal_method}{Marginal fitting method for the feature-level copula
texture model (mediator panel): \code{"auto"} (default, uses parametric
if KS test passes at p > 0.05, otherwise empirical), \code{"empirical"}
(always empirical CDF), or \code{"parametric"} (always best parametric
fit by AIC). See \code{\link[=train_feature_texture]{train_feature_texture()}}.}

\item{residualize_on}{Character: \code{"XC"} (default)
residualizes the Y/W correlation on exposure + covariates only;
\code{"XCW"} additionally residualizes on the W bridge proxy (PC1 of W),
reducing U-signature double-counting. Recommended only when the
completeness-capture test reports "strong".}

\item{Z_matrix}{Defunct. Renamed to \code{X_matrix}; passing a
value errors with a message pointing to \code{X_matrix}. Retained in the
signature only to catch and redirect old calls.}
}
\value{
A list with \code{gan_training_data} (sample x variable data frame with
exposure level, outcome level, mediator level, and encoded covariates),
\code{original_matrices} (\code{X}, \code{Y}, \code{M}, \code{W}), \code{covariates} (encoded frame),
\code{feature_correlations} (list with \code{Y}, \code{W} residual correlation matrices,
or \code{NULL} for absent panels), \code{feature_texture} (an
\code{iconic_feature_texture} object for the mediator panel, or \code{NULL} when
no M matrix is supplied), \code{sample_names}, \code{feature_names}, \code{n_samples},
\code{n_features}.
}
\description{
Accepts the exposure, outcome, mediator (optional), and negative-control
panels as \verb{features x samples} matrices together with a sample-level
covariate frame, and returns the tidy structures used by
\code{\link[=train_gan_on_real_data]{train_gan_on_real_data()}} and the calibration step of
\code{\link[=run_single_iteration]{run_single_iteration()}}.
}
\details{
In addition to the sample-level training frame (one row per sample with
exposure level, outcome level, and encoded covariates), the function
computes feature-level residual correlation matrices for each available
panel (Y, M, W). These capture the cross-feature correlation structure
that remains after removing the sample-level signal, so the simulation
can inject realistic correlated noise into the mediator and negative-
control panels.

If no matrices are supplied a small built-in example dataset is returned,
so the pipeline can be exercised end-to-end without real data.
}
