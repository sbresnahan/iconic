% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nc_diagnostics.R
\name{nc_completeness_check}
\alias{nc_completeness_check}
\title{Check negative-control completeness (dimensional + covariance-capture)}
\usage{
nc_completeness_check(
  dat,
  n_valid_controls = NULL,
  fdr_level = 0.1,
  n_cores = 1,
  outcome = "Y",
  n_perm = 1000,
  capture_thresholds = list(strong = 0.3, weak = 0.1)
)
}
\arguments{
\item{dat}{Dataset list from \code{\link[=run_single_iteration]{run_single_iteration()}} or
\code{\link[=generate_toy_data]{generate_toy_data()}}.}

\item{n_valid_controls}{Optional override: the number of valid
controls known from the study design. If
\code{NULL} (default), the function runs both
empirical screens and counts controls that
pass both.}

\item{fdr_level}{FDR level for the empirical screens (used
only when \code{n_valid_controls} is \code{NULL}).}

\item{n_cores}{Number of parallel workers for the empirical
screens. Default 1 (sequential).}

\item{outcome}{Outcome block for the capture test: \code{"Y"} (default)
or \code{"M"} (mediator). When \code{NULL}, the capture
component is skipped and only the dimensional
verdict is returned.}

\item{n_perm}{Number of permutations for the capture-test null.
Default 1000.}

\item{capture_thresholds}{Named list with \code{strong} and \code{weak} R^2 cutoffs
for the capture verdict. Default
\code{list(strong = 0.3, weak = 0.1)}.}
}
\value{
A list with:
\code{n_valid_controls} (count), \code{k} (number of confounders), \code{dim_W},
\code{dimensional} ("satisfied", "borderline", "under-identified"),
\code{capture} (output of \code{\link[=nc_completeness_capture]{nc_completeness_capture()}}, or NULL),
\code{completeness} (composite: "satisfied", "borderline",
"under-identified", or "weak-capture"),
\code{screen_X} (A1 screen results, if run), \code{screen_G} (A2 screen
results, if run).
}
\description{
Reports two components of the proximal-inference completeness condition:
}
\details{
(1) \strong{Dimensional} (the legacy count-based check): the number of valid
negative-control features vs the number of latent confounders k.
Bridge-function estimators (COCA, PGC) require at least as many
valid controls as confounders (Miao, Geng & Tchetgen Tchetgen,
2018). When \code{dim(W_valid) < k}, no estimator built on those
controls can recover the causal effect, regardless of sample size.

(2) \strong{Covariance-capture}: whether the controls actually
\emph{capture the confounder covariance} — i.e., whether adding W
reduces U's contribution to the outcome toward zero, operationalized
as the incremental R^2 of W for the outcome above covariates alone,
with a permutation null. This addresses the concern that
completeness is about covariance captured, not proxy number.

The composite \code{completeness} verdict requires the dimensional component
to pass (satisfied/borderline) AND the capture component to be non-
negligible (strong/weak). When the dimensional component passes but
capture is negligible, the verdict is "weak-capture".
}
\examples{
dat <- run_single_iteration(n_features = 10, n_confounders = 1, seed = 1)
nc_completeness_check(dat, n_perm = 50)
}
