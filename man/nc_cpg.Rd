% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nc_models.R
\name{nc_cpg}
\alias{nc_cpg}
\title{CpG-predicted-expression negative-control model (SCENIC case)}
\usage{
nc_cpg(U, covariates, params)
}
\arguments{
\item{U}{\verb{n x k} confounder matrix.}

\item{covariates}{Covariate data frame (unused; kept for the NC contract).}

\item{params}{List with \code{n_features}, and optionally \code{coverage}
(confounder->methylation strength, default 0.7), \code{captured} (confounder
indices, default all), \code{n_cpg} (methylation sites, default 60), \code{rho}
(AR(1) spatial correlation across sites, default 0.6), \code{MMCpG}
(methylation-confounding multiplier, default 1), \code{MMCon} (default 1),
and \code{noise_cor} (a \verb{p x p} correlation matrix for correlated
idiosyncratic noise, or \code{NULL} for independent noise).}
}
\value{
\verb{n x n_features} matrix of CpG-predicted negative controls.
}
\description{
Simulates spatially correlated CpG methylation whose signal is partly driven
by the captured confounders, then forms each negative control as a linear
prediction from the methylation sites ("CpG-predicted expression"). The
controls therefore carry confounder information only to the extent the
methylation does, mediated through a realistic spatial methylation layer.
}
\details{
When \code{noise_cor} is supplied (a \verb{p x p} correlation matrix), the
idiosyncratic noise added to each control is drawn from a multivariate
normal with that correlation structure, so the controls retain realistic
cross-feature correlations conditional on the confounder.
}
