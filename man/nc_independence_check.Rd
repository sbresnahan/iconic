% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nc_diagnostics.R
\name{nc_independence_check}
\alias{nc_independence_check}
\title{Test instrument-independence of negative controls (W \emph{|} G | C)}
\usage{
nc_independence_check(dat, fdr_level = 0.1, n_cores = 1)
}
\arguments{
\item{dat}{Dataset list from \code{\link[=run_single_iteration]{run_single_iteration()}} or
\code{\link[=generate_toy_data]{generate_toy_data()}}.}

\item{fdr_level}{Target FDR for BH correction. Default 0.10.}

\item{n_cores}{Number of parallel workers. Default 1 (sequential).
Uses \code{parallel::mclapply} on Unix and a PSOCK cluster on
Windows.}
}
\value{
A data frame with one row per control feature:
\code{feature}, \code{partial_r}, \code{p_value}, \code{fdr}, \code{significant}, \code{verdict}.
}
\description{
For each control feature, computes the partial correlation with the
genetic instrument G after residualising both on observed covariates
C, and reports the p-value. Controls significantly associated with G
after FDR correction may carry meQTL / allele-specific effects that
violate the instrument-independence assumption (A2).
}
\examples{
\dontrun{
dat <- run_single_iteration(n_features = 10, seed = 1)
nc_independence_check(dat)
}
}
