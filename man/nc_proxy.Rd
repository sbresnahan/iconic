% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nc_models.R
\name{nc_proxy}
\alias{nc_proxy}
\title{Direct-proxy negative-control model}
\usage{
nc_proxy(U, covariates, params)
}
\arguments{
\item{U}{\verb{n x k} confounder matrix.}

\item{covariates}{Covariate data frame (unused; kept for the NC contract).}

\item{params}{List with \code{n_features}, and optionally \code{coverage}
(scalar in \verb{[0, 1]}, default 0.7), \code{captured} (integer confounder indices
the controls see, default all), \code{noise_sd} (default 0.3), \code{MMCon}
(loading multiplier, default 1), \code{mode} (default \code{"shared"}), and
\code{noise_cor} (a \verb{p x p} correlation matrix for correlated noise, or
\code{NULL} for independent noise).}
}
\value{
\verb{n x n_features} matrix of negative controls.
}
\description{
Each control is a coverage-weighted mixture of the captured confounders plus
noise. With one confounder and \code{coverage = omega} this reduces to
the classic \code{w_signal} proxy in \code{\link[=generate_toy_data]{generate_toy_data()}}. Setting \code{captured} to a
strict subset of the confounders models controls that miss part of the
confounder subspace (invalidating the negative-control assumption).
}
\details{
When \code{noise_cor} is supplied (a \verb{p x p} correlation matrix), the noise
component is drawn from a multivariate normal with that correlation
structure, so the negative controls retain realistic cross-feature
correlations conditional on the confounder. When \code{noise_cor} is \code{NULL}
(default), the noise is independent across features.
}
\section{Modes}{

\describe{
\item{\code{"shared"}}{All columns carry the same \code{rowMeans(U[, captured])}
signal. This is the original behaviour (backward-compatible) and is
numerically stable, but the W matrix has only one effective dimension
regardless of \code{n_features}, so the proximal completeness condition is
never binding for the matrix-bridge PGC.}
\item{\code{"distinct"}}{Column \code{f} captures confounder
\verb{captured[((f - 1) \\\%\\\% length(captured)) + 1]}. Different columns
therefore carry signals from different confounders, giving the W
matrix genuine dimensional structure. This is the mode to use when
benchmarking the completeness cliff: the matrix-bridge PGC is
identified only when \code{n_features >= k}.}
}
}

