% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sensitivity.R
\name{nc_validity_check}
\alias{nc_validity_check}
\title{Check negative-control validity across confounding scenarios}
\usage{
nc_validity_check(
  trained_gan = NULL,
  coverage_grid = c(0.2, 0.5, 0.8, 1),
  k_grid = c(1, 2, 3),
  conf_strength = 0.8,
  n_valid_controls = 1,
  n_iter = 50,
  n_samples = 500,
  n_features = 20,
  nc_model = "proxy",
  base_seed = 800,
  n_cores = 1
)
}
\arguments{
\item{trained_gan}{An \code{iconic_gan} (or \code{NULL}).}

\item{coverage_grid}{Coverage values to sweep. Default \code{c(0.2, 0.5, 0.8, 1)}.}

\item{k_grid}{Numbers of confounders. Default \code{c(1, 2, 3)}.}

\item{conf_strength}{Fixed confounding strength. Default 0.8.}

\item{n_valid_controls}{Number of \emph{distinct valid} controls the design
provides (for the identifiability flag). Default 1.}

\item{n_iter, n_samples, n_features, nc_model, base_seed, n_cores}{As in
\code{\link[=gan_sensitivity]{gan_sensitivity()}}.}
}
\value{
A list with \code{summary} (COCA/PGC/IV2SLS bias & RMSE per scenario,
with an \code{identified} flag) and \code{verdict} (short per-scenario diagnosis).
}
\description{
Focuses on whether the negative-control estimators (COCA, PGC) hold as
the controls' coverage of the confounder subspace drops and as the number
of latent confounders grows. Reports their bias/RMSE alongside IV2SLS,
and flags scenarios where identification is not credible — in particular
when the number of latent confounders exceeds the effective number of
valid controls (the proximal-inference completeness condition).
}
\details{
The matrix-bridge PGC is the estimator for which the completeness condition
is binding: when \code{k > n_valid_controls}, PGC is under-identified while
IV2SLS remains unbiased (it does not depend on NC completeness).
}
\examples{
chk <- nc_validity_check(NULL, coverage_grid = 0.7, k_grid = 1,
  n_iter = 2, n_samples = 100, n_features = 5)
chk$verdict
}
