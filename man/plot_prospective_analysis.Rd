% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/figures.R
\name{plot_prospective_analysis}
\alias{plot_prospective_analysis}
\title{Prospective analysis figure}
\usage{
plot_prospective_analysis(prospect, file = NULL, width = 12, height = 8)
}
\arguments{
\item{prospect}{Result of \code{iconic_prospect()}.}

\item{file}{Optional file path to save the figure.}

\item{width}{Figure width in inches.}

\item{height}{Figure height in inches.}
}
\value{
A \code{patchwork} ggplot object.
}
\description{
Produces a 2-panel figure showing expected gains from adding
genetic instruments. Panel A: NDE bias vs instrument strength.
Panel B: prospective NDE/NIE estimates at target instrument
strength.
}
\examples{
if (check_torch_setup()) {
  data <- iconic_data(X = rnorm(100), Y = matrix(rnorm(100 * 10), 10, 100),
    M = rnorm(100))
  pros <- iconic_prospect(data, n_iter = 2, gan_epochs = 5,
    gamma_G_grid = c(0.4, 0.8), run_rho_sweep = FALSE)
  plot_prospective_analysis(pros)
}
}
