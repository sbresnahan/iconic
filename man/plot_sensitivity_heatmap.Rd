% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plots.R
\name{plot_sensitivity_heatmap}
\alias{plot_sensitivity_heatmap}
\title{Heatmap of a sensitivity metric across the scenario grid}
\usage{
plot_sensitivity_heatmap(
  sens,
  metric = "rmse",
  method = "IV2SLS",
  k = NULL,
  title = NULL
)
}
\arguments{
\item{sens}{Object returned by \code{\link[=gan_sensitivity]{gan_sensitivity()}}.}

\item{metric}{Column of \code{sens$summary} to map (e.g. "rmse", "abs_bias",
"power"). Default "rmse".}

\item{method}{Estimator to display. Default "IV2SLS".}

\item{k}{Number of confounders to slice on. Default: first in the grid.}

\item{title}{Optional plot title.}
}
\value{
Invisibly returns \code{NULL}; called for the side effect of drawing a sensitivity heatmap.
}
\description{
Draws a coverage x confounding-strength heatmap of a chosen metric for one
estimator, at a fixed number of confounders. Useful for reading off where a
method's bias/RMSE blows up.
}
\examples{
sens <- gan_sensitivity(NULL, conf_grid = c(0.5, 0.8),
  coverage_grid = c(0.3, 0.7), n_iter = 2, n_samples = 100,
  n_features = 5)
plot_sensitivity_heatmap(sens)
}
