% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/recommend.R
\name{print.iconic_recommendation}
\alias{print.iconic_recommendation}
\title{Print method for iconic_recommendation objects}
\usage{
\method{print}{iconic_recommendation}(x, ...)
}
\arguments{
\item{x}{An \code{iconic_recommendation} object.}

\item{...}{Unused.}
}
\value{
Invisibly returns \code{x} (the \code{iconic_recommendation} object); called for its side effect of printing a human-readable summary.
}
\description{
Print method for iconic_recommendation objects
}
\examples{
data <- iconic_data(X = rnorm(100), Y = matrix(rnorm(100 * 10), 10, 100),
  G = rnorm(100), W = matrix(rnorm(100 * 10), 10, 100))
diag <- iconic_diagnose(data)
est <- iconic_estimate(data, diagnosis = diag)
rec <- iconic_recommend(data, diagnosis = diag, estimate = est,
  auto_sensitivity = FALSE)
print(rec)
}
