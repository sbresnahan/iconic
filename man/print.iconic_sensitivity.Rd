% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model_sensitivity.R
\name{print.iconic_sensitivity}
\alias{print.iconic_sensitivity}
\title{Print method for iconic_sensitivity objects}
\usage{
\method{print}{iconic_sensitivity}(x, ...)
}
\arguments{
\item{x}{An \code{iconic_sensitivity} object.}

\item{...}{Unused.}
}
\value{
Invisibly returns \code{x} (the \code{iconic_sensitivity} object); called for its side effect of printing a human-readable summary.
}
\description{
Print method for iconic_sensitivity objects
}
\examples{
if (check_torch_setup()) {
  data <- iconic_data(X = rnorm(100), Y = matrix(rnorm(100 * 10), 10, 100),
    M = rnorm(100), G = rnorm(100), Gm = rnorm(100),
    W = matrix(rnorm(100 * 10), 10, 100))
  sens <- iconic_sensitivity(data, n_iter = 2, gan_epochs = 5,
    rho_G1_grid = c(0, 0.2), rho_G2_grid = c(0, 0.2))
  print(sens)
}
}
