% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/instruments_exposure.R
\name{qc_gwas_sumstats}
\alias{qc_gwas_sumstats}
\title{Standardize and QC GWAS summary statistics}
\usage{
qc_gwas_sumstats(
  sumstats,
  n_eff = NULL,
  drop_ambiguous = TRUE,
  beta_iqr_mult = 10,
  sd_ratio_range = c(0.5, 2),
  sd_y = NULL
)
}
\arguments{
\item{sumstats}{A data.frame of GWAS summary statistics. Recognized
column aliases: \code{chr}/\code{chromosome}/\code{CHR}; \code{pos}/\code{base_pair_location}/
\code{BP}; \code{a1}/\code{effect_allele}/\code{ALT}; \code{a0}/\code{other_allele}/\code{REF};
\code{beta}/\code{BETA}/\code{effect}/\code{logOR}; \code{beta_se}/\code{standard_error}/\code{se}/\code{SE};
\code{freq}/\code{effect_allele_frequency}/\code{EAF}/\code{FRQ}; \code{n_eff}/\code{n}/\code{N};
\code{p}/\code{p_value}/\code{P}; \code{rsid}/\code{rs_id}/\code{SNP}/\code{ID}.}

\item{n_eff}{Optional scalar: effective sample size, used when the
input has no sample-size column.}

\item{drop_ambiguous}{Logical: drop A/T and C/G SNPs whose strand
cannot be resolved. Default \code{TRUE}.}

\item{beta_iqr_mult}{Numeric: drop SNPs with
\eqn{|\beta - \mathrm{median}(\beta)| >} \code{beta_iqr_mult}
\eqn{\times \mathrm{IQR}(\beta)}. Default 10. Set to \code{Inf} to skip.}

\item{sd_ratio_range}{Numeric length-2: acceptable range for the
ratio of the summary-statistic-implied genotype SD to the
frequency-implied SD, \eqn{\sqrt{2 f (1-f)}}. SNPs outside the range
are dropped. Default \code{c(0.5, 2)} (LDpred2 tutorial). Requires \code{freq};
skipped with a message when frequencies are absent.}

\item{sd_y}{Optional scalar: phenotypic SD used in the SD-ratio check.
When \code{NULL} (default), estimated as the 1st percentile of
\eqn{\sqrt{0.5 (n_{eff} \cdot se^2 + \beta^2)}} across SNPs, the
continuous-trait approximation used in the LDpred2 tutorial.}
}
\value{
A list with:
\describe{
\item{sumstats}{data.frame of QC-passing variants with standardized
columns \code{chr}, \code{pos}, \code{a0}, \code{a1}, \code{beta}, \code{beta_se}, and (when
available) \code{n_eff}, \code{freq}, \code{p}, \code{rsid}.}
\item{qc}{data.frame with counts of input variants and variants
dropped by each filter.}
\item{sd_ratio}{numeric vector of SD ratios for retained variants,
or \code{NULL} when the check was skipped.}
}
}
\description{
Renames common GWAS summary-statistic column conventions to the
bigsnpr convention (\code{chr}, \code{pos}, \code{a0}, \code{a1}, \code{beta}, \code{beta_se},
\code{n_eff}, \code{freq}) and applies the standard LDpred2 QC filters:
missing/invalid values, extreme effect sizes, ambiguous-strand
(A/T and C/G) SNPs, and — when effect allele frequencies are
available — the standard-deviation ratio check from the LDpred2
tutorial.
}
\examples{
ss <- data.frame(
  chromosome = 1, base_pair_location = 1000 + 0:9 * 1000L,
  effect_allele = rep(c("A", "C"), 5), other_allele = rep(c("G", "T"), 5),
  beta = rnorm(10, 0, 0.05), standard_error = 0.01,
  effect_allele_frequency = runif(10, 0.1, 0.5)
)
out <- qc_gwas_sumstats(ss, n_eff = 50000)
str(out$sumstats)
out$qc
}
