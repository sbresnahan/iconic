% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mediation_simulation.R
\name{run_mediation_sim}
\alias{run_mediation_sim}
\title{Run repeated mediation simulations for a single parameter configuration}
\usage{
run_mediation_sim(
  n_iter = 100,
  n_samples = 500,
  n_features = 20,
  n_mediators = 1,
  beta_Z = 0.1,
  alpha_M = 0.5,
  beta_M = 0.3,
  conf_str = 0.8,
  w_signal = 0.7,
  mo_confounding = 0.8,
  phi = 0,
  rho_G1 = 0,
  rho_G2 = 0,
  rho_pop = 0,
  separate_U = FALSE,
  omega_1 = NULL,
  omega_2 = NULL,
  feat_cor = 0,
  base_seed = 100,
  n_cores = 1
)
}
\arguments{
\item{n_iter}{Number of simulation replicates. Default 100.}

\item{n_samples}{Observations per replicate. Default 500.}

\item{n_features}{Number of outcome and negative-control features. Default 20.}

\item{n_mediators}{Number of independent mediators (v0.8.4). When > 1,
each mediator has its own genetic instrument Gm and
contributes additively to Y. Default 1 (single mediator,
backward compatible).}

\item{beta_Z}{Direct effect of Z on Y (true NDE). Default 0.10.}

\item{alpha_M}{Effect of Z on mediator. Default 0.50.}

\item{beta_M}{Effect of mediator on Y (per-mediator true NIE = alpha_M * beta_M). Default 0.30.}

\item{conf_str}{Confounding strength delta. Default 0.80.}

\item{w_signal}{Proxy quality omega. Default 0.70.}

\item{mo_confounding}{Strength of U1 -> M (mediator-outcome confounding). Default 0.80.}

\item{phi}{Strength of the mediator instrument Gm -> M (v0.4.0).
0 = no mediator instrument (five estimators, backward
compatible).  > 0 = generates Gm and includes the
2-stage MR estimator (IV2SLS2). Default 0.}

\item{rho_G1}{Correlation of G1 with U_XM (v0.5.0). Default 0.}

\item{rho_G2}{Correlation of G2 with U_MY (v0.5.0). Default 0.}

\item{rho_pop}{Shared population structure (v0.5.0). Default 0.}

\item{separate_U}{Draw separate confounders for Z->M and M->Y paths (v0.5.0). Default FALSE.}

\item{omega_1}{Coverage of U_XM by W1 (v0.5.0). NULL = use w_signal.}

\item{omega_2}{Coverage of U_MY by W2 (v0.5.0). NULL = use w_signal.}

\item{feat_cor}{Within-module correlation for block-diagonal co-expression
modules in Y and W (v0.9.0). 0 = independent features. Default 0.}

\item{base_seed}{Starting seed; replicate i uses base_seed + i. Default 100.}

\item{n_cores}{Number of parallel workers. Default 1.}
}
\value{
A list with \code{summary} (one row per method with NDE/NIE bias,
RMSE, Type I), \code{raw} (full per-feature results), \code{true_NDE},
\code{true_NIE}, and \code{params}.
}
\description{
Generates \code{n_iter} synthetic datasets with mediator-outcome
confounding (via \code{mo_confounding}), runs all five (or six, when
\code{phi > 0}) mediation estimators, and summarises bias / RMSE / Type I
error for both NDE and NIE.
}
\details{
When v0.5.0 parameters are non-default (rho_G1, rho_G2, rho_pop,
separate_U, omega_1, omega_2), the two-stage proximal estimators (PGC2,
PGC2Gm) are also included.
}
\examples{
\donttest{
res <- run_mediation_sim(n_iter = 50, mo_confounding = 0.8)
res$summary
}
}
