% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mediation_simulation.R
\name{run_null_mediation_sim}
\alias{run_null_mediation_sim}
\title{Run null mediation simulations to estimate Type I error rates}
\usage{
run_null_mediation_sim(
  n_iter = 200,
  n_samples = 500,
  n_features = 20,
  beta_Z = 0.1,
  conf_str = 0.8,
  w_signal = 0.7,
  mo_confounding = 0.8,
  phi = 0,
  rho_G1 = 0,
  rho_G2 = 0,
  rho_pop = 0,
  separate_U = FALSE,
  omega_1 = NULL,
  omega_2 = NULL,
  feat_cor = 0,
  base_seed = 300,
  n_cores = 1,
  alpha = 0.05
)
}
\arguments{
\item{n_iter}{Number of replicates. Default 200.}

\item{n_samples}{Observations per replicate. Default 500.}

\item{n_features}{Features per replicate. Default 20.}

\item{beta_Z}{Direct effect (NDE, still active under null NIE). Default 0.10.}

\item{conf_str}{Confounding strength delta. Default 0.80.}

\item{w_signal}{Proxy quality omega. Default 0.70.}

\item{mo_confounding}{Strength of U1 -> M. Default 0.80.}

\item{phi}{Strength of the mediator instrument Gm -> M (v0.4.0).
0 = no mediator instrument. Default 0.}

\item{rho_G1}{Correlation of G1 with U_XM (v0.5.0). Default 0.}

\item{rho_G2}{Correlation of G2 with U_MY (v0.5.0). Default 0.}

\item{rho_pop}{Shared population structure (v0.5.0). Default 0.}

\item{separate_U}{Draw separate confounders (v0.5.0). Default FALSE.}

\item{omega_1}{Coverage of U_XM by W1 (v0.5.0). NULL = use w_signal.}

\item{omega_2}{Coverage of U_MY by W2 (v0.5.0). NULL = use w_signal.}

\item{base_seed}{Seed offset. Default 300.}

\item{n_cores}{Parallel workers. Default 1.}

\item{alpha}{Significance threshold. Default 0.05.}
}
\value{
A list with \code{rates} (data frame: method, NIE_type1, NDE_type1)
and \code{raw} (full results).
}
\description{
Sets \code{alpha_M = 0} and \code{beta_M = 0} (no true NIE) while
keeping \code{beta_Z} active (NDE still present).  Reports Type I
error for both NIE (should be 0 under the null) and NDE (should
reflect power for the direct effect).
}
