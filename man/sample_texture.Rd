% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gan.R
\name{sample_texture}
\alias{sample_texture}
\title{Draw synthetic base rows from a trained texture model}
\usage{
sample_texture(trained_gan, n)
}
\arguments{
\item{trained_gan}{An \code{iconic_gan} object from \code{\link[=train_gan_on_real_data]{train_gan_on_real_data()}}.}

\item{n}{Number of rows to draw.}
}
\value{
A data frame of \code{n} rows with the trained columns, on the original
(de-normalised) scale. Binary columns contain only 0/1 values.
}
\description{
Binary columns (detected at training time) are rounded to \eqn{{0, 1}} after
de-normalisation, and one-hot dummy groups are made mutually exclusive
(the column with the highest pre-rounding value wins per row).
}
\examples{
if (check_torch_setup()) {
  dat <- load_real_input_data(example = TRUE)
  gan <- train_gan_on_real_data(dat$gan_training_data, epochs = 5,
    verbose = FALSE)
  head(sample_texture(gan, 5))
}
}
