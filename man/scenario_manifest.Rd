% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generate_data.R
\name{scenario_manifest}
\alias{scenario_manifest}
\title{Scenario manifest: truth and parameter ranges for a simulation}
\usage{
scenario_manifest(
  dat_or_params,
  conf_grid = NULL,
  coverage_grid = NULL,
  mo_confounding_grid = NULL,
  phi_grid = NULL,
  rho_G1_grid = NULL,
  rho_G2_grid = NULL,
  rho_pop_grid = NULL,
  omega_1_grid = NULL,
  omega_2_grid = NULL
)
}
\arguments{
\item{dat_or_params}{A \code{dat} list from \code{\link[=generate_toy_data]{generate_toy_data()}} or a
named list of DGP parameters.}

\item{conf_grid}{Swept values of \code{conf_str} (confounding strength).
Default NULL.}

\item{coverage_grid}{Swept values of NC coverage (\code{w_signal} /
\code{omega}). Default NULL.}

\item{mo_confounding_grid}{Swept values of mediator-outcome
confounding strength. Default NULL.}

\item{phi_grid}{Swept values of the mediator-instrument strength
\code{phi}. Default NULL.}

\item{rho_G1_grid}{Swept values of the G1-conf_XM correlation. Default NULL.}

\item{rho_G2_grid}{Swept values of the G2-conf_MY correlation. Default NULL.}

\item{rho_pop_grid}{Swept values of the population-stratification
correlation. Default NULL.}

\item{omega_1_grid}{Swept values of \code{omega_1} (coverage of conf_XM by W1).
Default NULL.}

\item{omega_2_grid}{Swept values of \code{omega_2} (coverage of conf_MY by W2).
Default NULL.}
}
\value{
A named list with elements:
\describe{
\item{\code{estimands}}{Named numeric vector: \code{NDE}, \code{NIE}, \code{total}.}
\item{\code{modifiable_parameters}}{Data frame with columns
\code{parameter}, \code{value}, \code{swept_range}. \code{value} is the scalar
used (or NA when only a grid was supplied);
\code{swept_range} is a comma-separated string of grid values,
or NA when the parameter was held fixed.}
\item{\code{fixed_parameters}}{Data frame with columns \code{parameter},
\code{value}.}
}
}
\description{
Returns a reader-orienting summary of a simulation scenario: the
ground-truth estimands (NDE, NIE, total effect), the modifiable
parameters with their swept ranges, and the fixed parameters with
their values. Intended to be rendered as a table preceding simulation
results in the manuscript (, #582: "state the truth and the
parameter ranges up front to orient the reader").
}
\details{
\code{dat_or_params} may be either:
\itemize{
\item a list returned by \code{\link[=generate_toy_data]{generate_toy_data()}}, in which case the
estimands and fixed parameters are read from the object
(\code{true_NDE}, \code{true_NIE}, \code{true_total}, \code{n}, \code{n_features},
\code{n_mediators}, \code{lambda_XM}, \code{lambda_MY}, \code{feat_cor}); or
\item a bare named list of DGP parameters
(\code{beta_X}, \code{alpha_M}, \code{beta_M}, \code{n_mediators}, \code{n},
\code{n_features}, \code{lambda_XM}, \code{lambda_MY}, \code{feat_cor}, ...), in which case
the estimands are recomputed as
\code{NDE = beta_X}, \code{NIE = n_mediators * alpha_M * beta_M},
\code{total = NDE + NIE}.
}

The \verb{*_grid} arguments record the swept ranges for the modifiable
parameters. When a grid argument is omitted or NULL, the
corresponding modifiable parameter is reported with its scalar value
only (no range).
}
\examples{
dat <- generate_toy_data(n = 200, phi = 0.8, lambda_XM = c(1, 0), lambda_MY = c(0, 1),
omega_1 = 0.7, omega_2 = 0.7, seed = 42)
scenario_manifest(dat, conf_grid = c(0.3, 0.8),
coverage_grid = c(0.3, 0.7, 1))
}
