% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/instruments_exposure.R
\name{score_pgs_panel}
\alias{score_pgs_panel}
\title{Score a published polygenic-score panel on a dosage matrix}
\usage{
score_pgs_panel(weights, genotypes, impute_mean = TRUE, scale_score = TRUE)
}
\arguments{
\item{weights}{A data.frame of variant weights. Recognized columns:
\code{chr}/\code{chr_name}, \code{pos}/\code{chr_position}, \code{a1}/\code{effect_allele},
\code{a0}/\code{other_allele}, \code{weight}/\code{effect_weight}, and optionally
\code{rsid}. PGS Catalog scoring files can be read directly with
\code{\link[utils:read.delim]{utils::read.delim()}} (skip the header comment lines).}

\item{genotypes}{A numeric dosage matrix (variants x samples, dosages
in 0-2) whose rownames are either \code{chr:pos:ref:alt} keys or rsIDs
matching \code{weights}. A \code{\link[bigsnpr:snp_attach]{bigsnpr::snp_attach()}} \code{bigSNP} object is also
accepted (requires bigsnpr).}

\item{impute_mean}{Logical: mean-impute missing dosages per variant.
Default \code{TRUE}.}

\item{scale_score}{Logical: scale the resulting score to mean 0 /
sd 1. Default \code{TRUE}.}
}
\value{
A list with:
\describe{
\item{score}{named numeric vector of per-sample scores.}
\item{matched}{data.frame of matched variants with the weights
used (after any flip correction).}
\item{n_input}{number of weight-panel variants.}
}
}
\description{
Applies a published set of per-variant weights (e.g. a PGS Catalog
scoring file, which is already LD-aware) directly to a genotype
dosage matrix. Matching is by \code{chr:pos:ref:alt} key (with automatic
allele-flip handling: when the genotype stores the opposite
orientation, the weight is negated) or by rsID when present.
}
\examples{
# Toy dosage matrix: 4 variants x 50 samples
dos <- matrix(rbinom(4 * 50, 2, 0.3), nrow = 4,
              dimnames = list(c("1:100:A:G", "1:200:C:T",
                                "1:300:G:A", "2:400:T:C"),
                              paste0("S", 1:50)))
wts <- data.frame(chr = c(1, 1, 2), pos = c(100, 300, 400),
                  a1 = c("A", "G", "T"), a0 = c("G", "A", "C"),
                  weight = c(0.1, -0.2, 0.05))
out <- score_pgs_panel(wts, dos)
plot(out$score)
}
