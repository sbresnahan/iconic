% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/recommend.R
\name{summary.iconic_recommendation}
\alias{summary.iconic_recommendation}
\title{Summary method for iconic_recommendation objects}
\usage{
\method{summary}{iconic_recommendation}(object, ...)
}
\arguments{
\item{object}{An \code{iconic_recommendation} object.}

\item{...}{Unused.}
}
\value{
Invisibly returns \code{object}.
}
\description{
Prints the full recommendation summary (same as \code{print()}).
}
\examples{
data <- iconic_data(X = rnorm(100), Y = matrix(rnorm(100 * 10), 10, 100),
  G = rnorm(100), W = matrix(rnorm(100 * 10), 10, 100))
diag <- iconic_diagnose(data)
est <- iconic_estimate(data, diagnosis = diag)
rec <- iconic_recommend(data, diagnosis = diag, estimate = est,
  auto_sensitivity = FALSE)
summary(rec)
}
