# List the built-in negative-control models

List the built-in negative-control models

## Usage

``` r
list_nc_models()
```

## Value

Character vector of registered NC model names.

## Examples

``` r
list_nc_models()
#> [1] "proxy" "cpg"  
```
